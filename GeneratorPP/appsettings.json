{
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  },

  // support email
  "SupportMail": "podpora@ekosystem.slovensko.digital",

  // UseGenerator: Determines which generator to use.
  // Possible values: [External, anything else = Internal].
  "UseGenerator": "Internal",

  // Settings for external generator.
  // Known sub items: [Url,Username,Password,ServiceId,ServiceUserId].
  // See bysquare.com API documentation for details.
  "ExternalGenerator": {
    "Url": "https://app.bysquare.com/api/generateStringCodes"
    // Username,Password,ServiceId,ServiceUserId are considered secret.
    // Configure via UserSecrets in Development, and via Environment properties in other environments.
  }
}
